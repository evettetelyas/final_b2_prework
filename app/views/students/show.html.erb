<h1><%= @student.name %></h1>

<% @courses.each do |course| %>
    <section id="course-#{course.id}">
        <p><b><%= course.name %></b> | Grade: <%= course.find_student_grade(@student.id) %></p>
    </section>
<% end %>