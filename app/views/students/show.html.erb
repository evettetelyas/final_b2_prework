<h1><%= @student.name %></h1>

<% @courses.each do |course| %>
    <section id="course-#{course.id}">
        <p><b><%= course.name %></b> | Grade: <%= @student.find_grade(course).round(2) %></p>
    </section>
<% end %>